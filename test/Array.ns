--Created by Alex Crowley
--On August 2, 2016
--Copyright (c) 2016, TBD
--Licensed under the MIT license
--See LICENSE file for terms

include "test/Entity"

func main(){

    --Acts like ArrayList in Java
    var a ()int 
    --Can defer initialization, adhering to Declaration rules
    a = (1, 2, 3, 4, 5)

    var ents ()Entity = (Entity("a", 1, 1),Entity("a", 2, 2),Entity("a", 3, 3))

    --Arrays are 1 indexed due to Lua
    var b int = a[1]

    --Arry of array of int
    var c ()()int = ((1, 2), (3, 4))

    var d int = c[1][1]
}