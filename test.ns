--Code to test Nitro Scripting language

var y = (foo,bar){return 45}
var p = (){
	var x = (me){
	  me = (me + 2)
	  return me
	}
  return x
}

var nums = [[5],(){var x=2 print("WORKING!!!")},7,8,9,10]

var y = (2 * ( .10 + 20.3 * 5. ))
print(nums[1][1])

for i from 18 to 25 by 2 {
  print(i)
}
var nums = [5]
nums[1] = 2
print(nums[1])
for index,num in nums array {
  print("Testing for loop!")
}

class Entity { 
	
		x = 0, y = 0
		name
		
		Entity(name){
			this->name = name
			move(2,2)
		}
		
		move(x,y){
			this->x = x
			this->y = y
			alert()
		}	
		
		alert(){
			io.write("moved to: ")
			io.write(tostring(x))
			io.write(",")
			print(tostring(y))
		}
}

Entity e("Alice")
print(e->x,e->y)
e:move(5,6)
print(e->name)
print(e->x,e->y)

--Will only work if Object is bound in C/C++
cclass vec3{
    
    x, y, z

    vec3(x, y, z)
 
		cross(vec3)
    
		smult(scalar)
    
		rotate(angle, axis)
    
		normalize()

}


--vec3 vec(1,2,3)
--print(vec:>z)
var q = 11

if (q < 5 || q == 11) {
  print(q)
}
or q < 10 {
  print(q)
}
else {
  print("else statement")
} 
        switch(key){
            case left
                ld = true
                if y != 0 {
                    y = y * 0.5
                    x = -0.5
                }
                else { x = -1 }
            case right
                rd = true
                if y != 0 {
                    y = y * 0.5
                    x = 0.5
                }
                else { x = 1 }
						default
							print("fault")
        }